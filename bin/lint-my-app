root=$(npm explore lint-my-app -- pwd)

if [ -z "$1" ]
then
	sh $root/bin/lint
	exit $?
elif [ "$1" == "fix" ]
then
	sh $root/bin/fix
	exit $?
elif [ "$1" == "lint" ]
then
	sh $root/bin/lint
	exit $?
elif [ "$1" == "staged" ]
then
	sh $root/bin/staged
	exit $?
fi
