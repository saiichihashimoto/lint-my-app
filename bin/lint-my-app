#!/usr/bin/env bash
set -e

root=$(npm explore lint-my-app -- pwd >/dev/null 2>&1 || echo .)

if [ -z "$1" ]
then
	$root/bin/lint
elif [ "$1" == "fix" ]
then
	$root/bin/fix
elif [ "$1" == "lint" ]
then
	$root/bin/lint
elif [ "$1" == "staged" ]
then
	$root/bin/staged
fi
